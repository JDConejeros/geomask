% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/context_aware.R
\name{mask_context_aware}
\alias{mask_context_aware}
\title{Enmascaramiento con restricciones geográficas}
\usage{
mask_context_aware(
  data,
  method = mask_donut,
  boundary = NULL,
  forbidden = NULL,
  max_iter = 30,
  ...
)
}
\arguments{
\item{data}{Objeto \code{sf} POINT.}

\item{method}{Función de masking (p.ej. \code{mask_donut}).}

\item{boundary}{Polígono \code{sf} de área permitida.}

\item{forbidden}{Polígonos prohibidos (opcional).}

\item{max_iter}{Intentos por punto.}

\item{...}{Argumentos pasados a \code{method}.}
}
\value{
Objeto \code{sf} enmascarado.
}
\description{
Aplica un método de masking y re-muestrea ubicaciones inválidas (fuera de
límite o en zonas prohibidas).
}
\examples{
\donttest{
data(geomask_boundary, package = "geomask")
data(geomask_pts, package = "geomask")
pts <- sf::st_sf(geomask_pts[1:50, , drop = FALSE])
masked <- mask_context_aware(
  pts,
  method = mask_donut,
  boundary = geomask_boundary,
  min_r = 200,
  max_r = 800,
  seed = 5,
  max_iter = 5
)
}
}
\references{
Cassa, C. A., et al. (2006). A context-sensitive approach to anonymizing spatial
surveillance data. \emph{JAMIA}, 13(2), 160–165.
}
